{
  "entities": {
    "waitlist": {
      "title": "Waitlist",
      "description": "Emails of users interested in the Founder Plan during Alpha phase.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "User's email address."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the user joined the waitlist."
        },
        "source": {
          "type": "string",
          "description": "Where the user signed up from (e.g., 'hero', 'pricing')."
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/waitlist/{entryId}": {
      "schema": "waitlist",
      "description": "Collection of waitlist entries."
    }
  }
}
