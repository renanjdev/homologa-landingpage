import React from 'react';
import type { SectionProps } from '../contrato/types';

export default function PecaDensa({ label }: SectionProps) {
  return <div className="fable-placeholder">02 · {label}</div>;
}
