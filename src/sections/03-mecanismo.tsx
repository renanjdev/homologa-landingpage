import React from 'react';
import type { SectionProps } from '../contrato/types';

export default function Mecanismo({ label }: SectionProps) {
  return <div className="fable-placeholder">03 · {label}</div>;
}
