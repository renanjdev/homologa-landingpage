import React from 'react';
import type { SectionProps } from '../contrato/types';

export default function Virada({ label }: SectionProps) {
  return <div className="fable-placeholder">04 · {label}</div>;
}
