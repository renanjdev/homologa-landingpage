import React from 'react';
import type { SectionProps } from '../contrato/types';

export default function Prova({ label }: SectionProps) {
  return <div className="fable-placeholder">05 · {label}</div>;
}
