import React from 'react';
import type { SectionProps } from '../contrato/types';

export default function Acao({ label }: SectionProps) {
  return <div className="fable-placeholder">06 · {label}</div>;
}
